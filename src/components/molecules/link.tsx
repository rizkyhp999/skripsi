import React from "react";
interface data {
  children?: React.ReactNode;
}
export default function Navbar({ children }: data) {
  return <div>link</div>;
}

function sidebar() {
  return <div>link</div>;
}
